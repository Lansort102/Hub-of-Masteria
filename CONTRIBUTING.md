# Руководство по вкладу в проект

Спасибо за интерес к проекту Hub of Masteria! Мы рады любому вкладу.

## Как внести вклад

### Сообщение об ошибке

Если вы нашли баг:

1. Проверьте, не был ли он уже зарегистрирован в [Issues](https://github.com/yourusername/hub-of-masteria/issues)
2. Если нет, создайте новую Issue с шаблоном "Bug Report"
3. Опишите проблему максимально подробно:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Информация об окружении

### Предложение новой функции

1. Проверьте, не было ли это уже предложено
2. Создайте Issue с шаблоном "Feature Request"
3. Опишите:
   - Какую проблему решает функция
   - Как вы видите реализацию
   - Альтернативные решения

### Pull Request

1. **Форкните репозиторий**
2. **Создайте ветку** для ваших изменений:
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/your-bug-fix
   ```

3. **Следуйте стилю кода:**
   - Используйте осмысленные имена переменных
   - Комментируйте сложные части кода
   - Следуйте существующему стилю форматирования

4. **Тестируйте изменения:**
   - Убедитесь, что код работает
   - Проверьте на разных браузерах (если применимо)
   - Убедитесь, что не сломали существующий функционал

5. **Закоммитьте изменения:**
   ```bash
   git commit -m "Add: описание вашего изменения"
   ```
   
   Используйте префиксы:
   - `Add:` - новая функция
   - `Fix:` - исправление бага
   - `Update:` - обновление существующей функции
   - `Refactor:` - рефакторинг
   - `Docs:` - документация
   - `Style:` - форматирование

6. **Запушьте в ваш форк:**
   ```bash
   git push origin feature/your-feature-name
   ```

7. **Создайте Pull Request:**
   - Заполните шаблон PR
   - Опишите изменения
   - Укажите связанные Issues (если есть)

## Стандарты кода

### HTML
- Используйте семантические теги
- Всегда указывайте `alt` для изображений
- Используйте правильную структуру заголовков

### CSS
- Используйте CSS переменные для цветов и размеров
- Следуйте BEM-подобной методологии
- Комментируйте сложные селекторы

### JavaScript
- Используйте `const` и `let`, избегайте `var`
- Называйте функции осмысленно
- Добавляйте комментарии для сложной логики

## Процесс ревью

1. Все PR проходят ревью
2. Может потребоваться несколько итераций
3. Будьте открыты к конструктивной критике
4. Отвечайте на комментарии ревьюеров

## Вопросы?

Если у вас есть вопросы, создайте Issue с меткой `question`.

Спасибо за ваш вклад! 🎉

