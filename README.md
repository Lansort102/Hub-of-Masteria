# Hub of Masteria

[![CI](https://github.com/yourusername/hub-of-masteria/actions/workflows/ci.yml/badge.svg)](https://github.com/yourusername/hub-of-masteria/actions/workflows/ci.yml)
[![Deploy](https://github.com/yourusername/hub-of-masteria/actions/workflows/deploy.yml/badge.svg)](https://github.com/yourusername/hub-of-masteria/actions/workflows/deploy.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Веб-сайт сообщества Мастерия.

## 🚀 Быстрый старт

### Локальная разработка

1. Клонируйте репозиторий
2. Откройте `index.html` в браузере или используйте локальный сервер

### Docker

#### Запуск с Docker Compose

```bash
docker-compose up -d
```

Сайт будет доступен по адресу: http://localhost

#### Сборка образа вручную

```bash
docker build -t masteria-frontend .
docker run -p 80:80 masteria-frontend
```

## 📁 Структура проекта

```
Hub of Masteria/
├── index.html          # Главная страница
├── wiki.html           # Страница вики
├── history.html        # Страница истории
├── membersdesk.html    # Доска участников
├── style.css           # Стили
├── script.js           # JavaScript
├── img/                # Изображения
├── Dockerfile          # Docker образ
├── docker-compose.yml  # Docker Compose конфигурация
└── nginx.conf          # Конфигурация Nginx
```

## 🛠 Технологии

- HTML5
- CSS3 (с CSS переменными)
- Vanilla JavaScript
- Nginx (для production)
- Docker

## 📦 Деплой

### Автоматический деплой (GitHub Actions)

#### Настройка секретов в GitHub

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте следующие секреты:

**Обязательные для SSH деплоя:**
- `SERVER_HOST` - IP адрес или домен вашего сервера
- `SERVER_USER` - имя пользователя для SSH (например, `root` или `ubuntu`)
- `SSH_PRIVATE_KEY` - приватный SSH ключ для доступа к серверу

**Опциональные:**
- `SERVER_PORT` - порт SSH (по умолчанию 22)
- `DEPLOY_PATH` - путь к проекту на сервере (по умолчанию `/opt/masteria`)
- `DOCKER_USERNAME` - имя пользователя Docker Hub (для публикации образов)
- `DOCKER_PASSWORD` - пароль Docker Hub

#### Генерация SSH ключа

```bash
# На вашем локальном компьютере
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions

# Скопируйте приватный ключ в GitHub Secrets как SSH_PRIVATE_KEY
cat ~/.ssh/github_actions

# Добавьте публичный ключ на сервер
ssh-copy-id -i ~/.ssh/github_actions.pub user@your-server.com
```

#### Настройка сервера

```bash
# На сервере создайте директорию для проекта
mkdir -p /opt/masteria
cd /opt/masteria

# Клонируйте репозиторий
git clone https://github.com/yourusername/hub-of-masteria.git .

# Убедитесь, что Docker и Docker Compose установлены
docker --version
docker-compose --version
```

2. При пуше в `main` или `master` ветку автоматически запустится деплой

### Ручной деплой

```bash
# На сервере
git pull
docker-compose up -d --build
```

## 🔧 Настройка

### Переменные окружения

Создайте `.env` файл для настройки:

```env
NGINX_HOST=localhost
NGINX_PORT=80
```

### Nginx конфигурация

Настройки Nginx находятся в `nginx.conf`. Для изменения портов или добавления SSL, отредактируйте этот файл.

## 🗄 Бекенд

Бекенд будет добавлен позже. В `docker-compose.yml` есть закомментированные секции для:
- Backend сервиса
- Базы данных (PostgreSQL)

## 🤝 Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Создание Issue

Если вы нашли баг или хотите предложить новую функцию:
- Используйте шаблоны Issue в GitHub
- Опишите проблему или функцию максимально подробно
- Приложите скриншоты, если это возможно

## 📝 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 👥 Авторы

- **Ваше имя** - *Начальная работа* - [YourGitHub](https://github.com/yourusername)

## 🙏 Благодарности

- Сообщество Мастерия
- Все контрибьюторы проекта
